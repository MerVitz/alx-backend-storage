// my comment
db.school.deleteMany(
    { name: /ALX School/i },  // Using regex for case-insensitive matching of "ALX School"
    { multi: true }  // Ensure it deletes multiple documents if necessary
);
